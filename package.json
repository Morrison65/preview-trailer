{
  "name": "preview-trailer",
  "version": "1.1.0",
  "private": true,
  "description": "Open the selected page preview or trailer in a looping player",
  "scripts": {
    "check": "node --check preview-trailer.js && node --check update-shortkeys-from-github.js && node --test test/core.test.cjs test/update-shortkeys.test.cjs && node test/browser.cjs",
    "test": "node --test test/core.test.cjs",
    "test:browser": "node test/browser.cjs",
    "build": "node tools/build.cjs",
    "release": "npm run check && npm run build"
  }
}
